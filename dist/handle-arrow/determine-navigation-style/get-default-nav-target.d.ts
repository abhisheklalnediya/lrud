import { FocusState, Node, Orientation, Direction } from '../../types';
export default function getDefaultNavTarget(focusState: FocusState, node: Node, orientation: Orientation, direction: Direction): import("../../types").FocusNode | import("../../types").RootFocusNode | null;
